# Semua opsional — kalau tidak di-set, pakai default di src/config.js

# Confidence threshold (0-1)
VITE_CONF_THRESHOLD=0.4

# IoU threshold untuk NMS
VITE_IOU_THRESHOLD=0.45

# Jeda antar-inference dalam ms
VITE_INFERENCE_INTERVAL_MS=400
